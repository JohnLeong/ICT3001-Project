import React from "react";

export default function NavBar() {
  // Declare a new state variable, which we'll call "count"

  return (
    <div style={{backgroundColor: 'cyan', textAlign: 'center', lineHeight: '50px', height: '50px'}}>
        Maths Academy
    </div>
  );
}
